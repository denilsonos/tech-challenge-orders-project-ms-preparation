name: Node.js CI

on:
  pull_request:
    branches: [ "main" ]
    
jobs:
  CI:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x]

    steps:
    - uses: actions/checkout@v3

    - name: Using Node.js
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}
        
    - name: Run install and test
      run: |
        npm install
        npm run test
    
    - name: SonarQube Scan
      uses: sonarsource/sonarqube-scan-action@v1
      env:
        SONAR_TOKEN: "e6c014af7ff69f6d2af9e82683cdcd39c7793125"
        SONAR_HOST_URL: "https://c20c-2804-431-c7c8-b13e-34da-6790-32e1-1985.ngrok-free.app"
  
  